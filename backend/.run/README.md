# PyCharm Run Configurations
#
# Este diretório contém configurações de execução para o PyCharm.
# Elas são commitadas para que todos do time tenham o mesmo setup.
#
# Configurações disponíveis:
# - API (uvicorn): src/adapters/inbound/http/app.py
# - Worker: worker/main.py
# - Tests (all): pytest
# - Tests (unit): pytest -m unit
# - Tests (integration): pytest -m integration
